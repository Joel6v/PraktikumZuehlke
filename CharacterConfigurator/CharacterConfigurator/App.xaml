<Application x:Class="CharacterConfigurator.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CharacterConfigurator"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style x:Key="SkirymFont">
            <Setter Property="TextElement.FontFamily"
                    Value="pack://application:,,,View/Font/Balgruf-JRye7.ttf#Balgruf">
            </Setter>
        </Style>

        <Style x:Key="HoverOverText" TargetType="TextBlock">
            <Setter Property="Foreground">
                <Setter.Value>
                    <SolidColorBrush Color="Black"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <ColorAnimation To="CornflowerBlue"
                                        Duration="0:0:0.3"
                                        Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>

                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <ColorAnimation To="Black"
                                        Duration="0:0:0.3"
                                        Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GridBorderOutLine" TargetType="Border">
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2.5"/>
        </Style>

        <Style x:Key="StandardButton" TargetType="Button">
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="28"/>
        </Style>
        
        <Style x:Key="StatsTextBlockLeft" TargetType="TextBlock">
            <Setter Property="Margin" Value="12.5,12.5,0,0"/>
        </Style>

        <Style x:Key="ProgressBarStat" TargetType="ProgressBar">
            <Setter Property="Height" Value="12.5"/>
            <Setter Property="Margin" Value="15,18.5,-37.5,10"/>
            <Setter Property="Value" Value="100"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <Style x:Key="StatsTextBlockRight" TargetType="TextBlock">
            <Setter Property="Margin" Value="15,12.5,0,0"/>
        </Style>

    </Application.Resources>
</Application>
